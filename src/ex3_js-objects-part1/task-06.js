/* const getProperty = (object, propertyPath) => {
    const dot = '.';
    const anotherKey = propertyPath.split(dot);
    Object.entries(object).forEach(([key, value]) =>
    if (object[key, value] = anotherKey) {
        return anotherKey;
    }
    else {
        return undefined;
    }
} */
